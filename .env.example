# PostgreSQL (optional — defaults to SQLite when unset)
DATABASE_URL=postgresql://gitit:gitit@localhost:5432/gitit

# API auth (optional — dev mode when unset)
GIT_IT_API_KEY=

# LLM provider key
ANTHROPIC_API_KEY=

# GitHub context enrichment (optional)
GITHUB_TOKEN=
